# Development Environment Variables
# Copy this file to .dev.vars for local development secrets/overrides
# Most configuration now uses smart defaults

# Only add variables here that need to be secret or override defaults
# Examples:
# API_KEY=your-secret-api-key
# DATABASE_URL=your-custom-db-connection

# Optional: Override defaults if needed
# NODE_ENV=development
# WORKER_NAME=custom-worker-name
# BUCKET_NAME=custom-bucket-name
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# TRUSTED_ORIGINS=localhost:*

# Cloudflare credentials (for Drizzle Studio remote access)
# CLOUDFLARE_D1_ACCOUNT_ID=your_account_id
# CLOUDFLARE_DATABASE_ID=your_database_id
# CLOUDFLARE_D1_API_TOKEN=your_api_token