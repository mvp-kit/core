{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "Live demo of MVPKit - a modern full-stack TypeScript starter with React, tRPC, Cloudflare Workers, and authentication. Build and deploy MVPs faster.",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "preview": "turbo preview",
    "deploy:apps": "turbo run deploy:apps",
    "deploy:backend": "turbo run deploy:app --filter=@repo/api",
    "deploy:frontend": "turbo run deploy:apps --filter=@repo/web",
    "lint": "turbo lint",
    "lint:fix": "turbo run lint:fix",
    "format": "turbo format",
    "format:fix": "turbo run format:fix",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.6",
    "turbo": "2.6.1",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0",
    "bun": ">=1.0.0"
  },
  "packageManager": "pnpm@10.14.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "pnpm": {
    "overrides": {}
  }
}
